import { AsteriskMaskPipe } from './asterisk-mask.pipe';

describe('AsteriskMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new AsteriskMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
