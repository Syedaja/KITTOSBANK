import { SerchAccountsPipe } from './serch-accounts.pipe';

describe('SerchAccountsPipe', () => {
  it('create an instance', () => {
    const pipe = new SerchAccountsPipe();
    expect(pipe).toBeTruthy();
  });
});
